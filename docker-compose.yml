# File: docker-compose.yml (UNTUK PRODUKSI DENGAN COOLIFY)

version: '3.8'

services:
  backend:
    build:
      context: ./backend
    restart: always
    env_file:
      - ./backend/.env
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
    restart: always
    labels:                  
      - "coolify.expose.port=3000"
    depends_on:
      - backend

  db:
    image: postgres:15-alpine
    restart: always
    environment:
      - POSTGRAF_USER=${DB_USER}
      - POSTGRAF_PASSWORD=${DB_PASSWORD}
      - POSTGRAF_DB=${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  postgres_data:
